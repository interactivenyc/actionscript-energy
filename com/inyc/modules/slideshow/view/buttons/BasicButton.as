package com.inyc.modules.slideshow.view.buttons {	import com.inyc.utils.debug.Logger;		import flash.display.MovieClip;	import flash.events.MouseEvent;			/**	 * @author stevewarren	 */	public class BasicButton extends MovieClip {		//com.inyc.modules.slideshow.view.buttons.BasicButton		private var _selected:Boolean = false;						public function BasicButton(){			//log("PageButton CONSTRUCTOR");			this.buttonMode = true;			addEventListener(MouseEvent.ROLL_OVER, onRollOver);			addEventListener(MouseEvent.ROLL_OUT, onRollOut);			addEventListener(MouseEvent.CLICK, onClick);		}						private function onClick(e:MouseEvent):void{			if (!_selected) {				log("onClick");			}		}						private function onRollOver(e:MouseEvent):void{			if (!_selected) {				//log("onRollOver _selected:"+_selected);				gotoAndStop("over");			}		}				private function onRollOut(e:MouseEvent):void{			if (!_selected) {				//log("onRollOut _selected:"+_selected);				gotoAndStop("up");			}		}				public function set selected(pSelected:Boolean):void {			_selected = pSelected;			if (_selected) {				log("selected = "+this.name);				//try down first, and if there is a selected state, then go there				gotoAndStop("down");				gotoAndStop("selected");				buttonMode = false;			}else{				gotoAndStop("up");				buttonMode = true;			}		}				private function log(logItem:*,category:Array=null):void{			if(category==null) category = ["BasicButton"];			Logger.log(logItem,category,true);		}					}}