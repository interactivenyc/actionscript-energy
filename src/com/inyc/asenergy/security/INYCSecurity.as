package com.inyc.asenergy.security {	import com.inyc.asenergy.security.data.Policy;	import com.inyc.asenergy.security.data.PolicyDataProvider;	import flash.system.Security;	/**	* INYCSecurity 	*/		public class INYCSecurity {				public static function init() : void 		{			Security.allowDomain("*.homeunix.net");						Security.allowDomain("*.allthingsmedia.com");									Security.allowDomain("*");						Security.allowInsecureDomain("*");		}				public static function set policyFiles(dp:PolicyDataProvider) : void 		{			var i:int;        	var max:int = dp.length;        	var policy:Policy;			for (i=0; i<max; i++) {				policy = dp.getItemAT(i);				//ref = dp.getItemAT(i).ref;				Security.loadPolicyFile(policy.ref);			}		}			}	}